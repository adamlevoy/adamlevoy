---
import ProjectCard from "./ProjectCard.astro";

const {title} = Astro.props;
---

<div>
	<h2 class="text-4xl font-bold mt-8">{title}</h2>
	<div class="flex flex-wrap gap-4">
		<slot />
	</div>
</div>